export type TPlayerProps = {
  name: string;
  number: number;
  fg_per_cent: number,
  three_fg_per_cent:  number;
  points: number;
  redound: number;
  assist: number;
  about: string;
  imageLink: string;
  isReverse: boolean;
}