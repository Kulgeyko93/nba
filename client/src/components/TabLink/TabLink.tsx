import React from 'react';

// import { createStyles, makeStyles, Theme, Typography } from '@material-ui/core';

type TTab = {
  tabName: string;
};

const TabLink = ({ tabName }: TTab) => {
  return <div style={{ textDecoration: 'none' }}>NBA</div>;
};

export default TabLink;
